version=1.0.0
# Reuse task outputs, such as compiled classes, generated protobuf code and shaded JARs, from earlier builds.
# In GitHub Actions the local build cache is part of the Gradle User Home that setup-gradle saves and restores.
org.gradle.caching=true
